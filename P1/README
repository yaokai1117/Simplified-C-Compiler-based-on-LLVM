P1:

(1)、提交了四个使用C1语言写的小程序

可在bin/目录通过以下命令执行脚本run.sh自动编译运行（前三个程序）

	chmod +x run.sh
	./run.sh

源码在src/目录下

test1是一个简单的测试while循环语句的程序，会输出从0到9的数字

test2是一个简单的测试条件判定语句的程序，会输出1

test3是一个进行数组排序的程序，会输出排序前（逆序）和排序后的数组（从0到9）

test4是试验const数组声明中用const变量指明长度在GCC和Clang两种编译器下的不同编译结果，这个因为是看编译器的输出结果所以没有在shell脚本中编译运行


(2)、初步了解编译过程

写在doc/中


2015.9.9
