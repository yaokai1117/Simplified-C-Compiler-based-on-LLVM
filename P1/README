P1:

(1)、提交了三个使用C1语言写的小程序

可在bin/目录下执行脚本run.sh自动编译运行，源码在src/目录下

test1是一个简单的测试while循环语句的程序，会输出从0到9的数字

test2是一个简单的测试条件判定语句的程序，会输出1

test3是一个进行数组排序的程序，会输出排序前（逆序）和排序后的数组（从0到9）


(2)、初步了解编译过程

写在doc/中


2015.9.9
