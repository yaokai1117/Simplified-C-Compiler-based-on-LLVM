P5:

1.编译运行

	可使用脚本bin/run.sh自动编译并运行测试

	$bin/run

	也可make编译，手动运行，bin/compiler test/test1.c会在当前目录下生产test.ll中间码

2.完成情况
完成了7.2的要求，且完成了7.3的两个扩展，逻辑运算符和带参数的函数
另外写了一下错误处理，见下面第六个测试

3.测试样例说明
	test/test1.c------简单地测试if-else和while语句,会打印1和0 1 2 3 4 5 6 7
	test/test2.c------测试数组，会打印输出20151228和01234
	test/test3.c------测试函数（带参数），会打印6,2和它们的加减乘除
	test/test4.c------测试逻辑运算符,会打印2 200 1
	test/quicksort.c----一个快排程序
	test/error.c------含4个error，用以演示对代码生成时产生的错误的出来


