extension:

1.编译运行

	可使用脚本bin/run.sh自动编译并运行测试

	$bin/run

	也可make编译，手动运行，bin/compiler test/test1.c会在当前目录下生产test.ll中间码

2.完成情况
	在代码生成前加了一趟类型检查，可以报一些错。
	支持了浮点数和字符类型，多维数组，带参数和返回值的函数
	支持了结构体类型，指针类型（有一些不完善的地方）
	
3.测试样例的说明写在了脚本中
	


